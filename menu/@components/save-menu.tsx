import { MouseEventHandler } from "react";

interface SaveMenuProps {
  id: any;
  onClick: MouseEventHandler<HTMLButtonElement>;
}

export function SaveMenu({ onClick, id }: SaveMenuProps) {
  return <div></div>;
}
